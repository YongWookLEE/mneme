# step 5 — handoff-phase-07-queue

## 목적

라이브 검증 + 문서 갱신.

## 범위

- `docker compose up` → 메모리 생성 → `psql`로 `embedding` non-null + `usage_daily` 증가 확인.
- `docs/HANDOFF.md`/`CHANGELOG.md`/`docs/ROADMAP.md` 갱신.
- phase 07 (hybrid-search) 큐.

## Acceptance

- HANDOFF 마지막 업데이트가 phase 06 라이브 검증 통과로 기록.
